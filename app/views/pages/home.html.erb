  <div class="banner">
    <h1>The Morty you want is waiting for you pal !</h1>
  <br>
      <%= simple_form_for :search, url: morties_path, method: :get do |f| %>
    <div class="container">
      <div class="row">
        <div class="col-md-6">
        <%= f.input :name, placeholder: "Type this little brat's name" %>
        </div>
         <div class="col-md-6">
        <%= f.input :price, placeholder: "How much for him, you cheapfuck", collection: Morty::PRICE %>
        </div>
         <div class="col-md-6">
        <%= f.input :state, collection: Morty::STATE %>
        </div>
        <div class="col-md-6">
        <%= f.input :rarity, collection: Morty::RARITY %>
        </div>
      </div>
            <%= f.button :submit, "Find your new Morty", class: "home-btn" %>
    </div>
      <% end %>
      <br>
      <br>
      <h2> Lazy and hungover ? <%= link_to 'Scroll and find what we have !', morties_path(@morty) %> </h2>
    <br>
      <h3> Do you need some money after a crazy night on Pluton ?</h3>
      <h3> We have the solution: <%= link_to 'Share your Morty !', new_morty_path(@morty) %> </h3>
  <br>
      <h4> Check our latest Morties : </h4>
      <div class="container">
        <div class="cards-all">
          <% @morties.each do |morty| %>
            <%= link_to morty_path(morty) do%>
              <div class="card card-small" style="width: 18rem;">
                <%= image_tag morty.photo %>
                <div class="card-body">
                  <h5 class="card-title header-shadow-small"><%= morty.name%> </h5>
                  <p> <%= morty.state %></p>
                  <p><%= morty.rarity %></p>
                  <p><%= morty.price %></p>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
  </div>
