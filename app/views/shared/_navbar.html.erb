<div class="navbar navbar-expand-sm navbar-light navbar-lewagon" id="navbar-text">
  <%= link_to "/", class: "navbar-brand" do %>
    <%= image_tag "LogoRM.png" %>
    <% end %>

  <button class="navbar-toggler bg-light" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <p class="nav-link"> Welcome <%= current_user.first_name %> ! You dickhead 🖕
          <% current_user.morties.find do |morty| %> 
              <% if morty.reservations.last.present? && morty.reservations.last.pending? %>
                <%= link_to "🚨 You have a new reservation ! 🚨", my_morties_path, class: "notification" %> 
              <% end %>
          <% end %> 
            </p>
        </li>
        <li class="nav-item dropdown">
          <%= image_tag "RickAvatar.png", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "My Morties", my_morties_path, class: "dropdown-item" %>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link text-white" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
